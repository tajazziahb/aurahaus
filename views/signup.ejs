<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>BudgetBuddy | Sign Up</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;700&family=Poppins:wght@400;500;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>
<body class="auth-body">
    <div class="auth-wrapper">
        <div class="auth-card anim-fadeUp">
            <div class="auth-brand">
                <span class="brand-mark">$</span>
                <div class="brand-text brand-text-nowrap">BudgetBuddy</div>
            </div>

            <div class="auth-main">
                <h1 class="auth-title">Create your account</h1>
                <p class="auth-sub">Join BudgetBuddy and track your cash flow with ease.</p>

                <% if (message && message.length) { %>
                    <p class="auth-flash">
                        <%= message %>
                    </p>
                    <% } %>

                        <form action="/signup" method="POST" class="auth-form">
                            <div class="form-group">
                                <label class="form-label">Email</label>
                                <input class="form-control" type="email" name="email" required>
                            </div>

                            <div class="form-group">
                                <label class="form-label">Password</label>
                                <input class="form-control" type="password" name="password" required>
                            </div>

                            <div class="auth-actions">
                                <button type="submit" class="submit-button auth-submit">Create Account</button>
                            </div>
                        </form>

                        <p class="auth-footer-text">
                            Already have an account?
                            <a href="/login">Log in</a>
                        </p>
            </div>
        </div>
    </div>
</body>
</html>